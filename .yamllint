extends: default

rules:
  # 80 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 80
    level: warning

  empty-lines:
    max: 2
    max-end: 2
    max-start: 2

  document-start: disable

